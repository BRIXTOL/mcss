import { IRollupOptions } from './options';
export declare const cache: {
    mappings: {};
    stylesheets: {};
};
export declare const config: {
    available: {
        mcss: boolean;
        postcss: boolean;
    };
    options: IRollupOptions;
};
